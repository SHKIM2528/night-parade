# Git setup

Folder name:

```text
night-parade
```

Commands:

```bash
cd night-parade
git init
git branch -M main
git add .
git commit -m "chore: initialize night parade repository"
```

Do not run `npm create` yet. The rendering stack will be initialized after the first approved hero keyframe and Gate 01 implementation brief.
